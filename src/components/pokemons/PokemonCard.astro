---
interface Props {
  name: string;
  url: string;
  isBig?: boolean;
}

const { name, url, isBig = false } = Astro.props;
const id = url.split("/").at(-2);
const imageSrc = `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/${id}.png`;
---

<!-- href={`/pokemon/${id}`} -->
<a
  href={`/pokemons/${name}`}
  class:list={[
    `rounded-xl flex flex-col justify-center items-center p-2 hover:bg-slate-900 transition-all`,
    {
      border: !isBig,
    },
  ]}
>
  <img
    transition:name={`${name}-image`}
    src={imageSrc}
    alt={name}
    class:list={[
      `drop-shadow-lg`,
      {
        "size-[16rem]": isBig,
        "size-28": !isBig,
      },
    ]}
  />
  <span
    class:list={[
      `capitalize`,
      {
        "text-4xl text-blue-300": isBig,
      },
    ]}>#{id} {name}</span
  >
</a>
